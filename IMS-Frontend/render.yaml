services:
  - type: web
    name: ims-frontend
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npx serve@latest -s build -l 8080
    envVars:
      - key: REACT_APP_API_URL
        value: https://ims-backend-uik9.onrender.com
